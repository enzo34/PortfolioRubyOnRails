<div class="album text-muted">
  <div class="container">
    <div class="row sortable">
      <%= link_to "Créer un nouveau portfolio", new_portfolio_path if logged_in?(:site_admin) %>
      <%= render partial: 'portfolio_item', collection: @portfolio_items %>
      </div>
    </div>
  </div>
</div>